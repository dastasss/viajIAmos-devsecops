apiVersion: v1
kind: Service
metadata:
  name: driver-service
  namespace: viajiamos
  labels:
    app: driver-service
spec:
  type: ClusterIP
  selector:
    app: driver-service
  ports:
    - name: http
      port: 8003
      targetPort: http