apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: viajiamos
  labels:
    app: booking-service
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
    - name: http
      port: 8001
      targetPort: http