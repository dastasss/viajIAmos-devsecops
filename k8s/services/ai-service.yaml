apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: viajiamos
  labels:
    app: ai-service
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - name: http
      port: 8005
      targetPort: http